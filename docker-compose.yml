version: '3.8'

services:
  # ============================================
  # CORE: Evolution API (monitoring-only, lean)
  # ============================================
  evolution-api:
    image: evoapicloud/evolution-api:latest
    restart: unless-stopped
    # Coolify expõe a porta automaticamente ou via config de domain
    # Não defina container_name — Coolify gerencia
    ports:
      - "8080:8080"
    environment:
      # --- Core Server ---
      SERVER_TYPE: http
      SERVER_PORT: 8080
      SERVER_URL: ${SERVER_URL:-http://localhost:8080}

      # --- Auth ---
      AUTHENTICATION_API_KEY: ${AUTHENTICATION_API_KEY}
      AUTHENTICATION_EXPOSE_IN_FETCH_INSTANCES: false

      # --- Logging: Minimal ---
      LOG_LEVEL: ERROR,WARN
      LOG_COLOR: false
      LOG_BAILEYS: error
      EVENT_EMITTER_MAX_LISTENERS: 50

      # --- Database: External Postgres ---
      DATABASE_PROVIDER: postgresql
      DATABASE_CONNECTION_URI: ${DATABASE_CONNECTION_URI}
      DATABASE_CONNECTION_CLIENT_NAME: ${DATABASE_CONNECTION_CLIENT_NAME:-evolution_prod}

      # --- Persistence: NÃO salva mensagens (CRM é a fonte da verdade) ---
      DATABASE_SAVE_DATA_INSTANCE: true
      DATABASE_SAVE_DATA_NEW_MESSAGE: false
      DATABASE_SAVE_MESSAGE_UPDATE: false
      DATABASE_SAVE_DATA_CONTACTS: false
      DATABASE_SAVE_DATA_CHATS: false
      DATABASE_SAVE_DATA_LABELS: false
      DATABASE_SAVE_DATA_HISTORIC: false
      DATABASE_SAVE_IS_ON_WHATSAPP: false
      DATABASE_DELETE_MESSAGE: false

      # --- Instance Lifecycle ---
      DEL_INSTANCE: false

      # --- RabbitMQ: External ---
      RABBITMQ_ENABLED: true
      RABBITMQ_URI: ${RABBITMQ_URI}
      RABBITMQ_EXCHANGE_NAME: ${RABBITMQ_EXCHANGE_NAME:-evolution}
      RABBITMQ_GLOBAL_ENABLED: true
      # Apenas eventos essenciais
      RABBITMQ_EVENTS_MESSAGES_UPSERT: true
      RABBITMQ_EVENTS_CONNECTION_UPDATE: true
      RABBITMQ_EVENTS_INSTANCE_CREATE: true
      RABBITMQ_EVENTS_INSTANCE_DELETE: true
      # Todos os demais desabilitados explicitamente
      RABBITMQ_EVENTS_APPLICATION_STARTUP: false
      RABBITMQ_EVENTS_QRCODE_UPDATED: false
      RABBITMQ_EVENTS_MESSAGES_SET: false
      RABBITMQ_EVENTS_MESSAGES_EDITED: false
      RABBITMQ_EVENTS_MESSAGES_UPDATE: false
      RABBITMQ_EVENTS_MESSAGES_DELETE: false
      RABBITMQ_EVENTS_SEND_MESSAGE: false
      RABBITMQ_EVENTS_CONTACTS_SET: false
      RABBITMQ_EVENTS_CONTACTS_UPSERT: false
      RABBITMQ_EVENTS_CONTACTS_UPDATE: false
      RABBITMQ_EVENTS_PRESENCE_UPDATE: false
      RABBITMQ_EVENTS_CHATS_SET: false
      RABBITMQ_EVENTS_CHATS_UPSERT: false
      RABBITMQ_EVENTS_CHATS_UPDATE: false
      RABBITMQ_EVENTS_CHATS_DELETE: false
      RABBITMQ_EVENTS_GROUPS_UPSERT: false
      RABBITMQ_EVENTS_GROUP_UPDATE: false
      RABBITMQ_EVENTS_GROUP_PARTICIPANTS_UPDATE: false
      RABBITMQ_EVENTS_CALL: false
      RABBITMQ_EVENTS_TYPEBOT_START: false
      RABBITMQ_EVENTS_TYPEBOT_CHANGE_STATUS: false

      # --- Webhooks / Websocket / Cache: Disabled ---
      WEBHOOK_GLOBAL_ENABLED: false
      WEBHOOK_GLOBAL_URL: ''
      WEBHOOK_GLOBAL_WEBHOOK_BY_EVENTS: false
      WEBSOCKET_ENABLED: false
      WEBSOCKET_GLOBAL_EVENTS: false
      CACHE_REDIS_ENABLED: false
      CACHE_LOCAL_ENABLED: false

      # --- Session ---
      CONFIG_SESSION_PHONE_CLIENT: Evolution
      CONFIG_SESSION_PHONE_NAME: Chrome
      QRCODE_LIMIT: 30
      QRCODE_COLOR: '#175197'

      # --- Integrations: All disabled ---
      OPENAI_ENABLED: false
      DIFY_ENABLED: false
      FLOWISE_ENABLED: false
      N8N_ENABLED: false
      TYPEBOT_ENABLED: false
      CHATWOOT_ENABLED: false
      CHATWOOT_MESSAGE_READ: false
      CHATWOOT_MESSAGE_DELETE: false
      SQS_ENABLED: false
      PUSHER_ENABLED: false
      WA_BUSINESS_TOKEN_WEBHOOK: evolution
      WA_BUSINESS_URL: https://graph.facebook.com
      WA_BUSINESS_VERSION: v20.0
      WA_BUSINESS_LANGUAGE: pt_BR
      S3_ENABLED: false
      LANGUAGE: ${LANGUAGE:-pt_BR}

    volumes:
      - evolution_instances:/evolution/instances

    # Coolify / Compose standalone: mem_limit em vez de deploy.resources
    mem_limit: 3g
    mem_reservation: 512m

    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
        compress: "true"

  # ============================================
  # MONITORING: cAdvisor
  # ============================================
  cadvisor:
    image: gcr.io/cadvisor/cadvisor:v0.49.1
    restart: unless-stopped
    privileged: true
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker:/var/lib/docker:ro
      - /dev/disk/:/dev/disk:ro
    ports:
      - "8081:8080"
    command:
      - --housekeeping_interval=15s
      - --docker_only=true
      - --store_container_labels=false
      - --allow_dynamic_housekeeping=true
    mem_limit: 512m
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  # ============================================
  # MONITORING: Prometheus
  # ============================================
  prometheus:
    image: prom/prometheus:v2.53.0
    restart: unless-stopped
    ports:
      - "9090:9090"
    volumes:
      # MONTAR DIRETÓRIO, NÃO ARQUIVO (Coolify-friendly)
      - ./prometheus:/etc/prometheus:ro
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=15d'
      - '--storage.tsdb.retention.size=5GB'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'
      - '--web.enable-lifecycle'
    mem_limit: 2g
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  # ============================================
  # MONITORING: Grafana
  # ============================================
  grafana:
    image: grafana/grafana:10.4.3
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_ADMIN_USER:-admin}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD:-admin}
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_SERVER_ROOT_URL=${GRAFANA_ROOT_URL:-http://localhost:3000}
      - GF_INSTALL_PLUGINS=grafana-clock-panel
    volumes:
      # MONTAR DIRETÓRIOS, NÃO ARQUIVOS
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - ./grafana/dashboards:/var/lib/grafana/dashboards:ro
      - grafana_data:/var/lib/grafana
    mem_limit: 512m
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  evolution_instances:
    driver: local
  prometheus_data:
    driver: local
  grafana_data:
    driver: local
